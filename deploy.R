library(rsconnect)

# official version ----

deployApp(account = 'psrcwa',
         appName = 'planning-academy-centers-2024', # name of your app in the url https://psrcwa.shinyapps.io/dashboard-name
         appTitle = 'Summer Planning Academy Centers Dashboard') # name that will be displayed in the window title